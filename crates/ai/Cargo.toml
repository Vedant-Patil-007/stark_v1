[package]
name = "stark-ai"
version.workspace = true
edition.workspace = true

[dependencies]
stark-domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
regex = "1"

[dev-dependencies]
serde_json = "1"