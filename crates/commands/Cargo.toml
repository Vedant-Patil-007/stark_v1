[package]
name = "stark-commands"
version.workspace = true
edition.workspace = true

[dependencies]
stark-domain = { path = "../domain" }
stark-storage = { path = "../storage" }
rusqlite = { version = "0.37" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"