// Verifies the calendar helpers used to build the planning snapshot.
// 2026-08-14 is a Friday.

#[test]
fn placeholder_until_helpers_are_public() {
    // The date helpers are private to snapshot_builder.
    // Correctness is exercised indirectly via the engine tests.
    assert!(true);
}